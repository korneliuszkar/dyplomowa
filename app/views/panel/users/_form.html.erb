<%= form_for @user, url: panel_user_path(@user) do |f| %>
  Username: <%= f.text_field :name %><br />
  E-mail : <%= f.email_field :email, autofocus: true %><br />
  <%= f.collection_select :role, User::ROLES, :to_s, :humanize %>
  <%= f.submit "Submit"%>
<% end %>